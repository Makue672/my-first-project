# LibrarySystem (图书管理系统)

## 📖 项目简介

**LibrarySystem** 是一个基于 **C++ 链表**实现的图书管理系统。该项目旨在通过简单的控制台指令实现对图书数据的高效管理。

* **核心架构**：基于链表数据结构，实现动态内存管理。
* **主要功能**：支持图书的增、删、查、改（CRUD）操作。
* **数据持久化**：支持将图书数据保存至文件，并从文件中读取，保证数据不丢失。
* **多维搜索**：支持按不同关键字（书名、作者、ISBN等）进行精准查询。

---

## 🛠️ 运行环境

* **操作系统**：Windows 10 / Windows 11
* **编程语言**：C++ (Standard C++17)
* **IDE**：Visual Studio Code
* **编译器**：MinGW
* **构建工具**：CMake (Version >= 3.10)

---

## 📂 项目结构

```text
LibrarySystem/
├── CMakeLists.txt       # CMake 构建配置文件
├── include/             # 头文件目录
│   ├── Book.h           # 图书类定义
│   ├── BookList.h       # 链表操作类定义
│   └── globals.h        # 全局变量/常量
└── src/                 # 源代码目录
    ├── BookList.cpp     # 链表功能实现
    ├── menu.cpp         # 菜单界面逻辑
    └── main.cpp         # 主程序入口
````

-----

## 🚀 构建与安装

请按照以下步骤在您的本地环境中构建项目。

### 1\. 准备工作

确保您已安装 C++ 编译器（MinGW）和 CMake，并配置好环境变量。

### 2\. 配置 CMakeLists.txt

在项目根目录下创建 `CMakeLists.txt`，内容如下：

```cmake
cmake_minimum_required(VERSION 3.10)
project(LibrarySystem)

set(CMAKE_CXX_STANDARD 17)

include_directories(include)

add_executable(LibrarySystem 
    src/main.cpp 
    src/BookList.cpp 
    src/menu.cpp
)
```

### 3\. 编译项目

打开终端（Terminal），进入项目根目录，依次执行以下命令：

```powershell
# 1. 清理旧的构建文件夹 (如果在 PowerShell 中)
Remove-Item -Recurse -Force build -ErrorAction SilentlyContinue

# 2. 创建构建目录
mkdir build
cd build

# 3. 生成 Makefile (指定 MinGW 生成器)
cmake -G "MinGW Makefiles" ..

# 4. 编译生成可执行文件
cmake --build .
```

-----

## 💻 运行方式

编译成功后，可以通过以下方式运行程序：

1.  **终端运行**：
    ```bash
    ./LibrarySystem.exe
    ```
2.  **直接运行**：双击 `build` 文件夹下的 `LibrarySystem.exe`。

-----

## 📋 功能菜单

系统启动后，将显示以下主菜单指令：

| 指令 | 功能描述 |
| :---: | :--- |
| **1** | **添加图书** (向链表中添加新书) |
| **2** | **显示所有图书** (遍历打印链表) |
| **3** | **查询图书** (进入查询子菜单) |
| **4** | **修改图书信息** |
| **5** | **删除图书** (按指定条件删除) |
| **6** | **清空内存** (移除内存中所有节点) |
| **7** | **清空文件** (从磁盘文件中删除所有数据) |
| **0** | **退出系统** |

### 🔍 查询子菜单 (在主菜单选择 3 后进入)

  * `1`：按 **书名** 查询
  * `2`：按 **作者** 查询
  * `3`：按 **ISBN** 查询
  * `4`：按 **库内序号** 查询

-----

## 📝 开发日志

  * **初版完成**：实现了基础链表结构与文件读写功能。
  * **待优化**：增加输入合法性检测，优化UI显示。

-----

> Created by [Makue672]

```
```